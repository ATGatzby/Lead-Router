node_modules
.next
dist

# Generated Prisma client (regenerated by prisma generate)
packages/db/src/generated/
.turbo
*.env
*.env.local
.DS_Store

# Salesforce source tracking state
.sf/

# CLI intermediate + pack artifacts
apps/cli/*.tgz

# Customer install directory (generated by CLI init)
lead-routing/

# Claude Code state
.claude/

# Design preview / prototype HTML files
*-prototype.html
*-preview.html
*-redesign.html
routing-analysis*.md

# CLI test output
apps/cli/lead-routing/

# Internal documents — keep off GitHub
CLAUDE.md
Technical-Implementation.md
Deployment-Strategy.md
E2E-TEST-PLAYBOOK.md
E2ETest.md
Flowenhancement.md
NPXImplementation.md
investor-pitch.html
plans/
preview/
bugs.md
securityplan.md
RegressionSuite.md
docs/

# Flow Builder (not public yet — web UI only; engine flow-*.ts files are tracked)
apps/web/components/flow-builder/
apps/web/app/(dashboard)/flow-builder/
# NOTE: routing-rules/[id]/flow/ is NOT ignored — it's the Route Builder canvas (edit page)
apps/web/app/api/flows/
apps/web/lib/flow-*.ts
apps/mcp/src/tools/*-flow*.ts
# Flow Builder migrations are NOT gitignored — the schema has Flow Builder
# fields (routingMode, RoutingFlow, etc.) so the DB must have these columns
# even if the Flow Builder UI isn't shipped yet.
license-server/.wrangler/
